{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "homestead-dev",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["run", "dev"],
      "port": 5173,
      "//": "autoPort is false on purpose: the save lives in localStorage, which is scoped per origin. A different port is a different origin, so moving off 5173 makes existing progress look erased.",
      "autoPort": false
    }
  ]
}
